Exceptions are how a function says "I can't deal with this, somebody upstairs handle it." Something blows up three calls deep, you throw, and the error skips back up the stack until something is willing to catch it. The try-catch split is what makes the code readable. The happy path stays where you can actually see it, the recovery sits off to the side, and you stop having to thread an error code through every function in between.

Polymorphism lets you treat objects from different classes as if they were the same type, usually through a shared parent class or interface. The same method call can do different things depending on the actual object behind it. That's useful because you can write code against the general type and let each subclass handle the specifics, instead of branching on every possible case yourself.
